kind: Deployment
apiVersion: apps/v1
metadata:
  name: booking-java
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-java-app
  template:
    metadata:
      labels:
        app: booking-java-app
    spec:
      containers:
        - name: booking-java
          image: booking-java-image:latest
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
          resources:
            requests:
              memory: "180Mi"
              cpu: "10m"
---
kind: Service
apiVersion: v1
metadata:
  name: booking-java-svc
spec:
  ports:
    - name: http-8081
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: booking-java-app